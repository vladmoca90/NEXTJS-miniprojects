import { useState } from "react";
import NavbarComponent from "./counter-redux2/NavbarComponent";
import StoreComponent from "./counter-redux2/store/StoreComponent";

export default function Counter() {
    //const [cartCount, setCartCount] = useState(0);

    return (
        <>
            {/* <NavbarComponent cartCount={cartCount} />
            <StoreComponent setCardCount={setCardCount} /> */}
            {/* <Card1 cartCount={cartCount} />
            <Card2 cartCount={cartCount} />
            <Card3 cartCount={cartCount} /> */}
        </>
    );
}