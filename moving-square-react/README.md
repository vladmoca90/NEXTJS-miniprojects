# Moving Square (React)

Simple React app demonstrating a dark-red square that moves on click with a smooth animation.

Run locally:

```bash
cd moving-square-react
npm install
npm run dev
```

Controls:
- Click inside the box to move the square to the clicked spot.
- Click `Random` to jump to a random location.
- Click `Reset` to return to the starting corner.
